/**
 * Entry mode enum
 *
 * @since 2025-12-24
 */
export enum EntryMode {
  /**
   * Manual entry
   */
  MANUAL = 'MANUAL',

  /**
   * Swipe card
   */
  SWIPE = 'SWIPE',

  /**
   * Fallback swipe
   */
  FALLBACK_SWIPE = 'FALLBACK_SWIPE',

  /**
   * Contact chip
   */
  CONTACT = 'CONTACT',

  /**
   * Contactless
   */
  CONTACTLESS = 'CONTACTLESS',
}

